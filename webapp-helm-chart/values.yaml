# Default values for webapp-helm-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

different_images: 
  - registry.hub.docker.com/metarock/multiclient 
  - registry.hub.docker.com/metarock/multiserver